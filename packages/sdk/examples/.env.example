HUB_HTTP_URL=https://macbook-pro-de-olivier.local
HUB_TOKEN=CHANGE_ME_SUPERHUB_TOKEN

# Optional: custom CA file for local Caddy CA
HUB_TLS_CA_FILE="$HOME/.superhub-caddy-root.crt"

# Optional: disable TLS verification (dev only)
HUB_TLS_INSECURE=0

# Optional: stable client id
CLIENT_ID=
